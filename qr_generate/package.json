{
  "name": "qr-generator-serverless",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:frontend": "npm ci --prefix frontend",
    "build": "npm run install:frontend && npm --prefix frontend run build",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "nanoid": "^5.1.5",
    "zod": "^3.22.0"
  }
}
